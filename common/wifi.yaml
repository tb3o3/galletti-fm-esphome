wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  min_auth_mode: WPA2
  # Opzionale: per connessioni più veloci dopo il riavvio
  fast_connect: true

captive_portal:

web_server:
  port: 80
  version: 3
